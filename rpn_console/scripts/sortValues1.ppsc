@use(Array)
@use(Math)

do {
	clear	
	"Enter the size of an array: " print
	scan toNumber -> n
} until ( $n isNatural )

[] -> T
for ( 0 -> i ; i n < ; i ++ -> i ) {
	"Enter array value: " print
	$T scan toNumber push rem
	
}

"Sorted array: " print
$T sortNumbers println
