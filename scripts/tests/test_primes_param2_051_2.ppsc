scan toNumber >x

if ( $x 2 = $x 3 = or ) { 
	TRUE 
} else {
	TRUE -> guess
	for ( 2 -> i ; $i $i * $x <= guess and ; $i ++ >i ) {
		if ( $x $i mod 0 = ) {
			FALSE -> guess
		}	
	}
	$guess println
}
