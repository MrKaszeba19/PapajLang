// test for conditionals for for-loops without using brackets
@use(Number)

scan toNumber -> $x

if ( $x isEven )
	for ( 1 -> $i ; $i $x <= ; $i ++ -> $i ) {
		$i println
	}
else
	for ( $x -> $i ; $i 1 >= ; $i -- -> $i ) {
		$i println
	}
